# Whoa

Static GitHub Pages project site for `https://matt-scalcione.github.io/whoa/`.

## Files

- `index.html` - public landing page
- `styles.css` - route-safe styles loaded with relative paths
- `main.js` - lightweight client-side interactions
- `signal.html` - geolocation-aware observation bulletin page
- `signal.css` - styling for the observation bulletin
- `signal.js` - browser geolocation + localized bulletin copy

## Local preview

Open `index.html` directly, or serve the folder with any static file server.
